first

まず、scriptタグ内に$(function()){};を記述して、
処理を書いていく

問題1
id="q1"に対して、$('#q1')で紐付けを行う。 **idの場合は#で、クラスの場合は.で、〇〇()と書くものを「メソッド」
.css()で、カラーレッドに変える

問題2
id q2に対して、クリックされた時にCSSからバックグラウンドカラーをピンクで出力
$(this)は事前の$('#q2')のセレクタを意味する

問題3
id q2に対して、クリックされた時に関数が起動
.prepend()は指定した親要素の子要素の先頭に新しいコンテンツを追加するメソッド
.append()指定した子要素の最後にテキストを追加することができるメソッド
.before().after()前と後ろにテキスト追加

問題4
.addClass('large')でクリックされたら大きくなる

問題6
クリックされたら、animate()メソッドが起動
左に100px
上に100pxの隙間を作る様にDOMが動き
2000=2秒で処理が行われる

問題6
ulタグ内のliタグをクリックした際に.find('li')
.index()で兄弟要素の中で何番目にいるかを調べるメソッド
コンソールログとアラートでインデックス番号を表示

問題7
クリックした際に、data('text')テキストデータのデータオブジェクトを返す
コンソールログで出力

※ data属性について


second
クリックした際、クラスモーダルに紐付いて、起動してファイドインされる
buttonタグを押した際にフェードアウトされる。

※ クラス名のshow,content,closeは意味があるか


third
navクラス内のliがクリックされた際
クリックされたli要素が、兄弟要素の中で何番目かを取得し、変数indexに保存

全てのli要素から.removeClassでactiveの状態のクラスを閉じる、
.addClass('active')でクラスを追加
いったん、全てのコンテンツ（.contents li）に'is-hidden'クラスを追加して非表示にする
インデックス番号と同じ番目のコンテンツから'is-hidden'クラスを削除して表示する


fourth
menu list > liでホバー対象の選択
サブメニュー（.menu__list__childList）が slideDown() で表示
ホバーが外れた際に、サブメニュー（.menu__list__childList）が slideUp() で表示を閉じる
stop()はホバーが外れた際にアニメーションを止める役割
ないと、ホバーが外れても、アニメーションが最後まで実行される。


fifth
.selectで選んだvalueをselectedCategoryに代入
allだったら全てのliを返す

all以外が選択された場合、リストを非表示にして
data-category-typeとselectCategoryが一致するものを表示させる


sixth
最初にwrapper内でのクリック動作を3つにまとめている。

3つ目のresetGameに関して、リセットボタンとして関数化 function resetGame()

・handlePokemonSearch()の関数処理説明(ポケモンを探す機能)
pokeAPIからランダムでカントーポケモンのIDを受け取りたい条件を設定
ajaxでpokeAPIに接続し、ポケモンのIDを取得
取得データとしてimgとname

URLの中から日本語名があるか、確認して、存在する場合はpokemonNameに格納
HTMLに入れて表示


・handleCatchAttempt()関数処理に関して(捕まえるか逃げるか)
確率についてlet isGot = Math.random() < 0.5で50%で設定 if (isGot)で条件分岐 ゲットした時 ajaxでurlからモンスターボールの情報を取得 HTMLに入れて表示 失敗した時
    [逃げられてしまった...]を表示